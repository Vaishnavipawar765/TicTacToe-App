<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="16dp"
    android:background="@drawable/bg_gradient">

    <!-- Scoreboard -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp"
        android:layout_marginBottom="24dp"
        android:orientation="horizontal"
        android:gravity="center"
        android:background="@drawable/scoreboard">

        <TextView
            android:id="@+id/playerXScore"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Player X: 0"
            android:textSize="18sp"
            android:textColor="#FFFFFF"
            android:layout_marginEnd="24dp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/playerOScore"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Player O: 0"
            android:textSize="18sp"
            android:textColor="#FFFFFF"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- Game Board -->
    <GridLayout
        android:id="@+id/gridLayout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:columnCount="3"
        android:rowCount="3"
        android:layout_marginBottom="24dp">

        <!-- Buttons -->
        <Button android:id="@+id/button00" style="@style/GameTile" />
        <Button android:id="@+id/button01" style="@style/GameTile" />
        <Button android:id="@+id/button02" style="@style/GameTile" />
        <Button android:id="@+id/button10" style="@style/GameTile" />
        <Button android:id="@+id/button11" style="@style/GameTile" />
        <Button android:id="@+id/button12" style="@style/GameTile" />
        <Button android:id="@+id/button20" style="@style/GameTile" />
        <Button android:id="@+id/button21" style="@style/GameTile" />
        <Button android:id="@+id/button22" style="@style/GameTile" />
    </GridLayout>

    <!-- Status -->
    <TextView
        android:id="@+id/statusText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Turn: Player X"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="#FFEB3B"
        android:shadowColor="#000"
        android:shadowDx="1"
        android:shadowDy="1"
        android:shadowRadius="1"
        android:layout_marginBottom="16dp" />

    <!-- Reset Button -->
    <Button
        android:id="@+id/resetButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Reset Game"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:backgroundTint="#FF4081"
        android:paddingLeft="32dp"
        android:paddingRight="32dp" />
</LinearLayout>
